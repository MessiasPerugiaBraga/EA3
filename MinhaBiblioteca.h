#ifndef _TROCA_VETOR_H
#define _TROCA_VETOR_H

/*#define PHI 1.61803399
extern const double MEU_PI;
extern char minha_global;

typedef struct {
    double largura, comprimento;
}Retangulo;

typedef struct {
    double raio;
}Circulo;

double perimetro_retangulo(Retangulo);
double area_retangulo(Retangulo);
double perimetro_circulo(Circulo);
double area_circulo(Circulo);
*/

#endif
